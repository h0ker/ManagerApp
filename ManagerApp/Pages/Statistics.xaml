<Page
    x:Class="ManagerApp.Pages.Statistics"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ManagerApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="DarkGray">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.25*" />
            <RowDefinition Height="0.6*" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="0.3*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.1*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="0.1*" />
        </Grid.ColumnDefinitions>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/blurbackground.png" />
        </Grid.Background>
        <Button
            x:Name="uxBackButton"
            Style="{StaticResource NavigationBackButtonNormalStyle}"
            Grid.Row="0"
            Grid.Column="0" />

        <!-- Creating highlights bar-->
        <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            Grid.RowSpan="2"
            Background="Beige">
            
            <!--Title-->
            <StackPanel
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top"
                Background="Khaki">
                <TextBlock
                    Margin="10"
                    Foreground="Black"
                    FontSize="25"
                    FontWeight="SemiBold"
                    TextAlignment="Center"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Text="Restuarant KPI Highlights" />
            </StackPanel>
            
            <!--The list highlights-->
            <ListView
                x:Name="uxhightlightsGridView"
                IsItemClickEnabled="True"
                Background="Beige">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel
                            Orientation="Horizontal"
                            Width="300"
                            Background="White"
                            BorderBrush="Blue"
                            BorderThickness="2"
                            Margin="20">
                            <TextBlock
                                Margin="5"
                                Foreground="Black"
                                FontSize="20"
                                VerticalAlignment="Center"
                                Text="{Binding NameAndAmount}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <!-- Main stastistics view on right side-->
        <StackPanel
            Grid.Row="1"
            Grid.RowSpan="2"
            Grid.Column="2"
            Grid.ColumnSpan="3"
            Margin="70,0,70,0"
            Background="Beige">
            
            <!--Title-->
            <StackPanel>
                <ComboBox 
                x:Name="KPIComboBox" 
                PlaceholderText="Select view" 
                FontStretch="ExtraExpanded"
                FontSize="30"
                FontWeight="SemiBold"
                HorizontalContentAlignment="Center"
                VerticalAlignment="Stretch"
                HorizontalAlignment="Stretch" 
                SelectionChanged="KPIComboBox_SelectionChanged"
                Background="Khaki">
                    <x:String>Weekly View</x:String>
                    <x:String>Monthly View</x:String>
                    <x:String>Yearly View</x:String>
                </ComboBox>
            </StackPanel>
            
            <!--Monthly view-->
            <Popup
                x:Name="MonthlyViewPopup"
                IsOpen="False"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Opacity="50">
                <GridView x:Name="MonthlyViewGridView">
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="0.5*"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="0.5*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.5*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="0.5*"/>
                                </Grid.ColumnDefinitions>
                                <!--top preforming menu item-->
                                <StackPanel
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    Background="Aqua"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch">
                                    <TextBlock Text="Top Performing Menu Item" FontSize="15"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>

                </GridView>
            </Popup>


        </StackPanel>
        
        <!--popup for the yearly statistic view-->
        

    </Grid>
</Page>